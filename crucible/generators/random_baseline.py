"""Random-baseline `Generator` — rattled known JARVIS structure.

Used as a sanity-test counterpoint to learned generators: if our pipeline
ranks rattled-known structures higher than CrystaLLM samples, something is
wrong. Implemented in Phase 1.
"""
