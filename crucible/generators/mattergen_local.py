"""MatterGen `Generator` plugin — local pretrained diffusion model.

Wraps the Microsoft MatterGen Hydra-config CLI / Python API. Supports
conditional generation on chemistry, target properties, and space groups.
Heavier than CrystaLLM; CUDA required. Implemented in Phase 2.
"""
