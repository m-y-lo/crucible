"""`Orchestrator` with no LLM calls — hand-written state machine.

Zero Anthropic API cost. Used for Phase 3 central deployments where the
sponsor wants to keep operating cost minimal, and as a guarantee that the
discovery loop functions even without Claude. Implemented in Phase 2.
"""
