"""`crucible status` — read-only run report.

Prints the current run's budget consumption, gauntlet histogram (per stage:
passed / failed and top fail reasons), and the top-10 ranked structures
with composition, score, and key predicted properties. Implemented in
Phase 1.
"""
